use pyo3::prelude::*;
use pyo3::types::PyList;
use std::fs;

fn find_semantic_duplicated_by_ai_model(&mut self) {
    // compare ast for semantic
    // compare the structures among functions
    // then return the % 
    // data flow approach or CFG?

}
